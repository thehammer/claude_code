# Claude Code - Credentials Template
# Copy this file to ~/.claude/credentials/.env and fill in your API tokens
# NEVER commit the actual .env file - it's gitignored for security

# ============================================================================
# JIRA
# ============================================================================
# Create API token: https://id.atlassian.com/manage-profile/security/api-tokens
# Permissions needed: Read/write issues, comments
JIRA_API_TOKEN=your_jira_token_here
JIRA_EMAIL=your.email@company.com
JIRA_DOMAIN=yourcompany.atlassian.net

# ============================================================================
# CONFLUENCE
# ============================================================================
# Uses same token as Jira if on Atlassian Cloud
CONFLUENCE_API_TOKEN=${JIRA_API_TOKEN}
CONFLUENCE_EMAIL=${JIRA_EMAIL}
CONFLUENCE_DOMAIN=yourcompany.atlassian.net

# ============================================================================
# BITBUCKET
# ============================================================================
# Create app password: https://bitbucket.org/account/settings/app-passwords/
# Permissions needed: Repositories (read/write), Pull requests (read/write)
BITBUCKET_USERNAME=your_username
BITBUCKET_APP_PASSWORD=your_app_password_here
BITBUCKET_WORKSPACE=your_workspace

# ============================================================================
# GITHUB
# ============================================================================
# Create personal access token: https://github.com/settings/tokens
# Permissions needed: repo, workflow, read:org
GITHUB_TOKEN=your_github_token_here
GITHUB_USERNAME=your_username

# ============================================================================
# SENTRY
# ============================================================================
# Create internal integration: https://sentry.io/settings/[org]/developer-settings/
# Permissions needed: project:read, event:read, org:read
SENTRY_API_TOKEN=your_sentry_token_here
SENTRY_ORG=your_organization

# ============================================================================
# DATADOG
# ============================================================================
# Create API key: https://app.datadoghq.com/organization-settings/api-keys
# Create APP key: https://app.datadoghq.com/organization-settings/application-keys
DATADOG_API_KEY=your_api_key_here
DATADOG_APP_KEY=your_app_key_here
DATADOG_SITE=datadoghq.com  # or datadoghq.eu for EU

# ============================================================================
# SLACK
# ============================================================================
# Create bot token: https://api.slack.com/apps
# Permissions needed: chat:write, channels:read
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_WORKSPACE=your_workspace

# ============================================================================
# AWS (if using AWS integrations)
# ============================================================================
# These may already be configured via ~/.aws/credentials
# Only needed if you want Claude to use different credentials
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1

# ============================================================================
# NOTES
# ============================================================================
# - All tokens should be kept secret
# - Rotate tokens periodically (every 90 days recommended)
# - Use bot/service accounts where possible (not personal accounts)
# - Grant minimum required permissions
# - Revoke tokens when no longer needed
# - This file is gitignored - verify before committing anything
